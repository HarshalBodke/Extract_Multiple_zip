# Extract_Multiple_zip
extract all files from multiple zip files in separate directories 

Set the path of zip files.
Run the code.
All zip files will be extracted to respective named folders/directories.
